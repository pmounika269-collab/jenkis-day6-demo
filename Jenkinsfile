pipeline {
    agent any

    stages {
        stage('Day 11 Test') {
            steps {
                echo 'Hello from Jenkinsfile'
                sh 'whoami'
                sh 'pwd'
            }
        }
    }
}
